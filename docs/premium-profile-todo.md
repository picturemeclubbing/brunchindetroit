# Premium Profile To-Do

## Future admin settings

Add premium profile background controls in admin settings:

- Premium page background:
  - color picker default: `#faf6ee`
  - optional background image upload / URL
- Premium content shell background:
  - color picker default: `#fefefc`
  - optional background image upload / URL
- Premium alternating section background:
  - color picker default: `#fcfaf6`
- Premium CTA background:
  - color picker default: `#14201e`
- Premium footer background:
  - color picker default: `#15231f`

These should later be saved through site settings or deployment settings so the premium layout can be themed without editing CSS.
